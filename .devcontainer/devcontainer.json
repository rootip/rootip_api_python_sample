{
  "name": "Python CLI",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  },
  "postCreateCommand": "python -m pip install --only-binary=:all: --require-hashes -r requirements-lock.txt && python -m unittest discover -s tests",
  "features": {}
}
