# rootip API × AIコーディング セキュリティガイド

このガイドは、rootip APIのサンプルコードをAIコーディングツール（Claude Code、GitHub Copilot、ChatGPT等）で活用・改変する方向けに、安全に開発するための考え方と手順をまとめたものです。プログラミングの専門家でない方を主な読者として想定しています。

## なぜこのガイドが必要か

AIにコード作成を支援させる「AI支援コーディング」は便利ですが、生成されたコードをよく確認せずに実行すると、次のような事故につながることがあります。

1. **偽パッケージのインストール（サプライチェーン攻撃）**
   AIが実在しない、または攻撃者が用意した紛らわしい名前のパッケージ（タイポスクワッティング）を提案することがあります。`pip install` した瞬間に悪意あるコードが実行され、PCの情報やAPIトークンが盗まれる可能性があります。

2. **秘密情報の漏えい**
   APIトークンやパスワードをAIチャットへ貼り付けたり、AIがコード内へ直書きしたコードをそのまま公開したりすると、第三者があなたの権限でrootipのデータへアクセスできてしまいます。

3. **通信の安全性の無効化**
   TLS証明書エラーが出たとき、AIは「`verify=False` を付ければ動きます」と提案しがちです。動きはしますが、通信の暗号化検証が無効になり、盗聴や改ざんに気づけなくなります。

4. **意図しないデータの一括更新・削除**
   AIが生成した更新処理が誤っていた場合、確認なしで実行すると大量のデータを壊す可能性があります。知財管理データは事業の重要資産です。

5. **プロンプトインジェクション**
   Webページ、CSV、メール等の「データ」の中に、AIへの偽の指示文が仕込まれていることがあります。AIがそれを本物の指示と誤解し、意図しない操作を行う攻撃です。

このリポジトリは、上記の事故を減らすための補助的な対策をコードと設定で用意しています。これらは人による確認や組織のセキュリティ対策を置き換えるものではありません。

## このリポジトリに組み込まれている安全対策

| 対策 | 内容 | 関連ファイル |
|---|---|---|
| 依存パッケージの固定 | バージョンとSHA-256ハッシュで固定し、すり替えを検知 | `requirements.txt` / `requirements-lock.txt` |
| 秘密情報の分離 | `config/secrets.py` はGit管理外。テンプレートのみ公開 | `config/secrets.py.template` / `.gitignore` |
| TLS検証の常時有効化 | `verify=False` を使用しない。社内CAは `ROOTIP_CA_BUNDLE` で対応 | `rootip/api.py` |
| 接続先の制限 | https必須・`*.rootip-cloud.net`・rootip APIのパス配下のみ。リダイレクトを追跡しない | `rootip/api.py` |
| DELETEの禁止 | サンプルの共通処理からDELETEは実行不可 | `rootip/api.py` |
| 更新前の人による確認 | 更新内容を表示し、yes入力後にのみ実行。一括更新は件数上限つき | `rootip/app_sample/master_currencies_get_and_update.py` / `master_staffs_put.py` |
| 通信タイムアウト | 応答しない接続で処理が止まり続けることを防止 | `rootip/api.py` |
| CSVインジェクション対策 | Excelで数式として解釈される値を無害化して出力 | `rootip/api.py` |
| AI向けルールの常時適用 | 主要AIツールが起動時にセキュリティルールを読み込む | `AI_DEVELOPMENT_RULES.md` ほか |
| ガードレールのテスト | URL・メソッド・CSV等の主要ガードの一部を自動確認 | `tests/` |

## 安全に使うための5つの約束

### 約束1: パッケージは指定の方法でインストールする

READMEに記載された次のコマンドを使ってください。ダウンロードされたファイルがロックファイルに記録したSHA-256ハッシュと一致するかをpipが検証します。ハッシュ一致はパッケージ自体の安全性を保証しません。

```bash
python -m pip install --only-binary=:all: --require-hashes -r requirements-lock.txt
```

AIが「このパッケージを追加しましょう」と提案してきた場合は、すぐにインストールせず次を確認してください。

- そのパッケージは本当に必要か（標準機能で代替できないか）
- PyPI公式サイト（https://pypi.org）でパッケージ名を正確に確認したか（1文字違いの偽物に注意）
- 公開から日が浅すぎないか、メンテナンスされているか

### 約束2: 秘密情報をAIに渡さない

- APIトークン・パスワード・証明書は `config/secrets.py` にのみ記入します。
- AIチャット、プロンプト、ソースコード、スクリーンショットへ秘密情報を貼り付けないでください。
- 「トークンを教えてください」とAIが求めてきても渡さないでください。正しく作られたプログラムは、実行時に `config/secrets.py` から自動で読み込みます。
- 漏えいの可能性があれば、rootipの画面からトークンを失効・再発行してください。

### 約束3: エラーの「近道」に乗らない

AIはエラーを解決するために、安全機能を無効化する提案をすることがあります。次の提案が出たら、実行せずにルール違反として扱ってください。

- 「`verify=False` を追加すれば動きます」
- 「ハッシュチェックを外してインストールしましょう」
- 「確認プロンプトを削除して自動化しましょう」
- 「管理者権限で実行してください」

### 約束4: 更新系の処理は「確認してから実行」

- まずGET（読み取り）だけで動作を確認してください。
- 更新（PUT/POST）を伴うプログラムは、必ず「何をどう変えるか」を画面に表示してから実行される作りを保ってください。
- 本番データではなく、rootipのデモ・検証環境で試すことを推奨します。
- 一度に大量更新しない（通貨一括更新サンプルでは上限50件に設定）。
- 一括更新は途中で失敗すると一部だけ成功する可能性があります。完了件数を確認し、自動再送しないでください。

### 約束5: AIの成果物は人がレビューする

- AIが書いたコードを実行する前に、少なくとも「どこと通信するか」「何を書き換えるか」「何をインストールするか」の3点を確認してください。
- 判断できない場合は実行せず、社内のIT担当者や rootip サポートへ相談してください。
- `python -m unittest discover -s tests` でガードレールのテストが通ることを確認してください。

## AIコーディングツールへのルール適用のしくみ

このリポジトリには、対応するAIコーディングツールが読み込む設定ファイルを同梱しています。ツールや設定によっては自動適用されないため、正本の `AI_DEVELOPMENT_RULES.md` を人が確認してください。

| ファイル | 対象ツール |
|---|---|
| `AI_DEVELOPMENT_RULES.md` | 全ツール共通の正本ルール |
| `CLAUDE.md` | Claude Code |
| `AGENTS.md` | OpenAI Codex等、AGENTS.md対応ツール |
| `.github/copilot-instructions.md` | GitHub Copilot |
| `.cursor/rules/rootip-security.mdc` | Cursor |

ただし、**これらのルールはAIへの「お願い」であり、強制力はありません**。AIがルールを破る可能性は常に残るため、上記の「5つの約束」による人の確認と、コード側の仕組み（ハッシュ固定・DELETE拒否・確認プロンプト等）を併用することが重要です。

## 万一の際の連絡・対処

1. APIトークンや証明書が漏えいした可能性がある場合：rootipの画面からトークンを失効・再発行し、管理者へ連絡してください。
2. 意図しないデータ更新をしてしまった場合：追加の操作をせず、変更内容（対象・件数・時刻）を記録し、[root ip公式サイト](https://rootip.co.jp)のサポート窓口へ相談してください。
3. 不審なパッケージをインストールしてしまった場合：そのPCでの作業を止め、社内のIT担当者へ連絡してください。

## このガイドの限界

- ここに記載した対策は事故の可能性を下げるものであり、ゼロにするものではありません。
- APIトークン自体が強い権限を持つ場合、コード側の制限は補助的な対策です。トークンの管理と定期的な再発行が重要です。
- 本番データを扱う自動処理を作る場合は、組織のセキュリティ担当者によるレビューを受けてください。
- テストはすべての安全要件を証明するものではありません。CIと人によるレビューを併用してください。
